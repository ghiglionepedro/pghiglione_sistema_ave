<odoo>

    <!-- Animal groups -->
    <record id="group_mascotas_manager" model="res.groups">
        <field name="name">Administrador</field>
        <field
            name="users"
            eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"
        />
    </record>

</odoo>
